# Contributing

Keep indexing local, deterministic, and dependency-free. Ranking changes need small corpora that demonstrate both the intended result and a near-miss. Avoid reading files other than `SKILL.md` during discovery.
